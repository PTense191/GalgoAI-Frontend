Agrega el .env de whatsapp en /galgoai-web

Corre dentro de la carpeta /galgoai-web lo siguiente:

npm install
npm install next-auth @next-auth/prisma-adapter @prisma/client
npm install prisma --save-dev
npm run dev
